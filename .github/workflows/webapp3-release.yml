name: 'webapp3 release'

on:
  push:
    branches:
      - main
    paths:
      - 'webapp3/**'

jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
